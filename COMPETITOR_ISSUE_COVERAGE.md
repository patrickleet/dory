# Competitor issue coverage

Last refreshed from the live GitHub issue trackers on 2026-07-14.

This is a release-risk checklist, not marketing copy. The Apple Silicon launch has no partial
support tier: an advertised capability is either **FULL** and protected by a mandatory release
gate, or it is a **LAUNCH BLOCKER** and cannot be marketed or shipped as complete. A blocker may be
implemented but awaiting exact-artifact/environment certification, or may still require product
work; the closure column says which. Intel-host support is a later product track and is not counted
as Apple Silicon incompleteness. The common-amd64-image contract uses Dory's bundled FEX runtime;
untested x86-only products remain outside that advertised contract.

The notarized 0.3.0/18 Apple Silicon candidate built from
`06e8c77592cc318e971529ada0ec3a93d67bb40a` has now passed the complete 34-row bounded
Docker/runtime campaign with its exact bundled Docker CLI and standalone engine: all 34 rows passed,
including concurrent backpressure, named signals, 2,000 forwarded connections, 20 restarts,
host-port collision/recovery, Compose and engine restart continuity, bind/FD/hard-link behavior,
healthchecks, DNS/search isolation, named-volume copy, BuildKit concurrency, image archive/import,
and cleanup persistence. The retained evidence at
`/tmp/dory-exact-runtime-evidence-20260714T064643Z` is bound to the candidate commit and every
runtime payload hash; manifest SHA-256 is
`aeb84256209d52e4ddbdc245c62942c3f02452d1dda6d4c45d5e253baa5474bb`. This removes the bounded
runtime uncertainty from the affected rows. It does not waive rows whose closure also requires the
final tagged rebuild, eight/25-hour duration, a clean app/update install, physical sleep/networking,
or external APFS media.

The same exact candidate standalone archive also passes cached offline boot with its bundle sources
hidden, complete managed-drive persistence and transient-runtime replacement, 16→128 GiB sparse
growth with real discard reclaim and restart persistence, cross-container BSD/POSIX advisory locks,
and SSH-agent forwarding through one plus eight concurrent clients and a required network-disabled
BuildKit mount. The SSH proof binds both candidate Docker and Buildx hashes and retains no public or
private key text. Installed direct/Sparkle app paths, physical VZ restart, uninstall, external-drive,
and final-duration requirements remain blockers where the table explicitly calls for them.

| Competitor failure class | Dory coverage | Remaining release action |
|---|---|---|
| OrbStack [Wi-Fi failure after sleep](https://github.com/orbstack/orbstack/issues/702), Colima [sleep failure](https://github.com/abiosoft/colima/issues/460), Rancher Desktop [socket failure after sleep](https://github.com/rancher-sandbox/rancher-desktop/issues/1274) | **LAUNCH BLOCKER — exact physical certification**. Wake/clock repair and bounded lifecycle tests exist. The notarized direct-candidate workflow now binds the exact app/Docker/doryd/dory-hv hashes and commit/run/attempt, schedules a hardware wake, physically sleeps the Apple-Silicon Mac five times, and requires unchanged default route, scoped DNS/search state, proxies, `/etc/resolver`, associated Wi-Fi, an active VPN, the configured corporate resolver, internal host/container HTTPS reachability, and Docker API recovery after every wake. Publication semantically rejects immediate-return sleep, missing schedule/pmset logs, incomplete/failed cycles, changed network contracts, different candidate binaries, or a different private network contract. | Execute the five-cycle gate on the dedicated physical release Mac while its corporate split-DNS VPN is active. It requires the exact `SLEEP-AND-WAKE-THIS-MAC` token and passwordless `pmset` only on that clean runner; ordinary development tests never sleep the user's Mac. |
| OrbStack [mount/server disconnects](https://github.com/orbstack/orbstack/issues/1342), [whole-VM wedge on a blocking file-share open](https://github.com/orbstack/orbstack/issues/2593), and [missing bind watcher events](https://github.com/orbstack/orbstack/issues/2561); Lima [inotify gaps](https://github.com/lima-vm/lima/issues/615) and [mode-0000 exclusive-create orphan](https://github.com/lima-vm/lima/issues/5039); Colima [whole-home inotify drives `fseventsd` memory/CPU](https://github.com/abiosoft/colima/issues/1569); Docker Desktop [bind-mount performance](https://github.com/docker/for-mac/issues/77), [kernel crash while switching VirtioFS to gRPC FUSE](https://github.com/docker/for-mac/issues/7825), [nested bind regression](https://github.com/docker/for-mac/issues/7868), and [anonymous subvolume regression](https://github.com/docker/for-mac/issues/7869) | **LAUNCH BLOCKER — certification**. Host-share exact-tree, FSEvents loss/recovery, repeated npm churn, host-to-guest inotify, and host/guest write tests pass. Dory retains arbitrary bind reachability but no longer subscribes FSEvents to all of `$HOME` or `/Volumes`: a guest lookup synchronously arms only the accessed top-level project/volume root before its namespace stat, and additional roots use independent streams without restarting existing observation. Dory deliberately has one in-process HostFS/virtiofs implementation and no runtime file-share-backend selector, so it cannot unmount VirtioFS and load an out-of-tree gRPC FUSE module during a backend transition; a static release test rejects any production `grpcfuse` integration. The exact runtime gate now proves nested child-bind precedence, independent parent/child writes, and anonymous child-volume isolation from the host parent. HostFS pins regular-file, directory, and symlink identities using non-blocking event-only descriptors; it rejects unsupported host FIFOs/devices/sockets during lookup because Linux can block on FIFO semantics before issuing FUSE OPEN. The guest watcher now uses non-truncating write access for mode-0200 regular files and falls back to a live parent for mode-000 paths instead of repeating a permanently denied read until coherence recovery restarts the VM. Dory also preserves the creating descriptor for an `O_CREAT + O_EXCL + O_RDONLY` mode-0000 open instead of failing after creation and leaving an inaccessible host orphan; the exact current-source non-root container reproduction creates, verifies mode 0000, and unlinks successfully. The full-default current-source gate completed mode-0200 creation, 10,000 bind operations with zero FD growth, and 1,000 restrictive hard-link reads without a nudge failure or VM restart. The mandatory live gate also proves a host FIFO fails within five seconds while unrelated Docker requests stay live. The endurance harness includes bind/inotify churn and attributed host `fseventsd` RSS/CPU budgets. | Complete eight hours on the exact final signed/notarized candidate, including the special-file/nested-mount/mode-0000 gates, narrow-root observation evidence, host `fseventsd` plateau, graceful single-backend shutdown/restart, and physical sleep/wake. Do not claim a bind-mounted npm performance win until the benchmark campaign supports it. |
| Rancher Desktop [bind-mount `chown` failure](https://github.com/rancher-sandbox/rancher-desktop/issues/1209), Apple container [restrictive bind create failure](https://github.com/apple/container/issues/1344), [intermittent hard-link permission failures](https://github.com/apple/container/issues/1937), and [virtiofs silently drops `nosuid` mount options](https://github.com/apple/container/issues/1940) | **FULL** — virtiofs virtualizes container UID/GID while leaving host APFS ownership unchanged; refresh, hard-link alias, restrictive create, and host-replacement regressions pass. The exact candidate proved `chown 999:999`, access from a second UID 999 container, unchanged host ownership 501:20, and mode-0200 create/write. The runtime gate performs 1,000 restrictive-mode reads through both hard-link aliases. Dory advertises Docker's documented bind syntax rather than Apple's undocumented kernel-flag grammar: unsupported `:nosuid` now has a mandatory bounded regression requiring an explicit validation error and no partial container, while supported `:ro` must remain `RW=false` in inspect, preserve reads, reject a guest write, and leave the host untouched. The current-source 28-row campaign passed both semantics (`~/.dory-new-gate-evidence/20260713T-competitor-runtime-28-row-mount-options`, archive SHA-256 `2cad44e4c264dea5202de462df25bf15723f3c0c4939f00a998ace39f6db3b39`). | Keep the regression mandatory. Never accept and silently discard a mount option. Virtual ownership is engine-lifetime state, so images must continue their normal entrypoint ownership setup after an engine restart. |
| Docker Desktop [multiple threads can acquire the same exclusive `flock()` on VirtioFS](https://github.com/docker/for-mac/issues/7004) | **LAUNCH BLOCKER — exact-artifact certification**. Current source implements owner-isolated Linux `GETLK`/`SETLK`/`SETLKW` plus BSD-flock semantics with independent Darwin open descriptions. Blocking requests use bounded nonblocking retries, accept `FUSE_INTERRUPT`, and release on unlock, flush, direct release, process death, and connection reset. Protocol tests cover shared/exclusive conflicts, disjoint and partial ranges, blocking acquisition, query, cancellation, and cleanup. The digest-pinned current-source two-container gate now passes exclusive/shared exclusion, explicit unlock, Linux-compatible non-atomic upgrade failure plus retry, non-overlapping POSIX ranges, blocking wakeup, and forced-container crash release. The isolated qualifier binds the fixture to its actually shared engine HOME, and publication rejects a missing exact Docker CLI digest. | Repeat the same mandatory gate through both exact notarized app paths and long qualification before this row becomes FULL. Preserve Linux `flock(2)` semantics: conversion is not guaranteed atomic, so a failed nonblocking upgrade must not be misrepresented as retaining the prior shared lock. |
| OrbStack [external-volume virtiofs FD exhaustion](https://github.com/orbstack/orbstack/issues/2592) and [capacity wraps above 16 TiB](https://github.com/orbstack/orbstack/issues/2532), Apple container [external APFS ownership failures](https://github.com/apple/container/issues/1830) | **LAUNCH BLOCKER — physical external-drive certification**. Current source mounts `/Volumes` at the identical guest path in both app and standalone runtimes, so an explicit Docker bind no longer becomes a guest-only path. HostFS sources capacity from Darwin's native 64-bit `fstatfs`, never the 32-bit-count `statvfs` compatibility structure; a focused regression carries block/file counts above `UInt32.max` through the FUSE response without wrap. The mandatory gate crosses the reported leak threshold with 10,000 bind-file operations and enforces a bounded aggregate Dory FD budget. It now refuses to detach any volume lacking the exact operator-created dedicated-release marker, logically unmounts that external APFS device, proves a missing-drive bind fails within ten seconds without creating an internal shadow path or wedging Docker, remounts the same device, and rechecks bytes and hard links. Nothing is exposed inside a container unless the user explicitly requests that bind path. Share construction rejects relative, root-overlay, traversal, NUL-containing, and malformed hidden-name inputs; the sensitive-name denylist is case-insensitive across lookup, enumeration, mutation, and host-event mapping so case-insensitive APFS cannot expose `.SSH` around a `.ssh` policy. | Attach and explicitly mark a dedicated writable external APFS drive, then run ownership, hard-link, 64-bit capacity, 10,000-open/FD, 64 MiB checksum, disconnect/reconnect, engine-restart, and missing-drive fail-closed cases on both exact notarized app paths. This Mac currently has no writable external test drive attached. |
| Lima [disk corruption](https://github.com/lima-vm/lima/issues/1957), OrbStack [disk/state failure](https://github.com/orbstack/orbstack/issues/1331) and [cold-start failure from missing data-directory TCC permission](https://github.com/orbstack/orbstack/issues/2580), Colima [unsupported `fstrim` breaks provisioning](https://github.com/abiosoft/colima/issues/1549), and Apple container [fatal startup on an unwritable app root](https://github.com/apple/container/issues/1802) and [silent reuse of a daemon from a different app root](https://github.com/apple/container/issues/1757) | **FULL** — all workload data is rooted in one relocatable `.dorydrive`; every engine takes a nonblocking, symlink-resistant exclusive lock before attaching it. The managed-drive gate proved images, stopped containers, writable layers, volumes, and networks survive total replacement of `~/.dory`; it also proves missing external drives and concurrent attachment fail closed. Internal roots are confined to Dory's Application Support directory. Desktop, Documents, Downloads, iCloud, CloudStorage, and arbitrary home paths fail before launch because a separately launched daemon cannot rely on the app's transient TCC grant; `/Volumes` roots must be mounted local APFS. An unwritable selected root exits normally with a path-specific permission error, leaves no partial bundle, and does not disturb the running Docker API. If `data-drive.path` is lost while the engine remains healthy, the launcher restores it only after matching the live dory-hv PID, private state/socket arguments, and exact `--data-drive … --kernel` boundary; a different requested drive fails closed without creating or modifying that bundle. The current-source live gate passed those cases, both graceful `powerOff` events, and the complete persistence/replaced-runtime cycle (`~/.dory-new-gate-evidence/20260713T-data-drive-identity-fix-final3`). Dory advertises discard only through its own implemented virtio-blk path, and the growth gate requires actual discard reclaim plus successful boot-time trim rather than treating an unsupported trim as success. | Keep lost-metadata same-drive recovery, wrong-drive rejection, unwritable-root, alternate-drive non-mutation, protected/unsupported-location, local-APFS, 16→128 GiB growth, trim, restart, and lock gates mandatory on the exact arm64 artifact. Homebrew uninstall and `--zap` must preserve the drive. |
| OrbStack [missing Docker export/backup](https://github.com/orbstack/orbstack/issues/2354), [migration source-data loss](https://github.com/orbstack/orbstack/issues/1530), [empty migrated Postgres data](https://github.com/orbstack/orbstack/issues/1585), and [sparse disk truncation after macOS Migration Assistant](https://github.com/orbstack/orbstack/issues/2472); Colima [images and containers lost while volumes survived an upgrade](https://github.com/abiosoft/colima/discussions/1420) | **LAUNCH BLOCKER — exact clean-v1 artifact certification**. The shared plan → quiesce → stage → verify → publish → validate executor is now the product path. Real OrbStack import passes all 68 image, writable-layer, volume, network, container-definition, state, fixed-port, and exact-baseline tests. Full-drive backup/restore uses the same durable journal and lock, content-addressed read-back-verified chunks, sparse extents, an atomic completion marker, exclusive no-overwrite publication, external-volume rebinding, exact restored-tree inventory, and resumable publication ownership. Eight adversarial library tests and the real `dory data` CLI sparse/xattr round trip pass. | Repeat import and clean-v1 backup/verify/restore through the exact notarized arm64 app; restore across the dedicated external APFS drive, run interrupted/truncated/bad-chunk and Time Machine/Migration Assistant campaigns, and retain hashes plus physical-allocation evidence. |
| OrbStack [open-FD growth](https://github.com/orbstack/orbstack/issues/2347), Lima [one socket FD leaked per forwarded connection](https://github.com/lima-vm/lima/issues/5210) | **LAUNCH BLOCKER — final-artifact certification**. The completed eight-hour baseline recorded 3,969 passing cycles over exactly 28,800 seconds with stable resource medians. The bounded candidate gate also held aggregate engine FDs at 65→65 across 2,000 forwarded connections and 20 restarts; publication now requires that exact named row, and bind-file churn has its own 10,000-operation FD budget. Long qualification holds display-neutral idle/system sleep assertions, samples AC power every minute, and publication rejects missing, shortened, or battery-powered assertion history. | Repeat the eight-hour campaign on the exact final notarized candidate; later source changes supersede the completed baseline for publication. |
| Apple containerization [process-wide proxy hang under concurrent backpressure](https://github.com/apple/containerization/issues/712) and Apple container [process kill always fails from a cross-OS signal wire mismatch](https://github.com/apple/container/issues/1941) | **LAUNCH BLOCKER — final-artifact certification**. Dory's 30-row standalone runtime gate opens six unread 8 MiB log streams to force front-side socket backpressure while twelve unrelated Docker control requests run concurrently. Every control request has a deadline. Its Docker signal contract sends the name `USR1` across the host/Linux boundary rather than a Darwin signal number; the gate requires that name to reach both container init and a detached exec process while init and the Docker API remain live. The latest current-source run passed all 30 rows with 2,000 connections, 20 restarts, bounded unrelated probes, full engine stop/start recovery, and three graceful guest `powerOff` events (`~/.dory-new-gate-evidence/20260713T-competitor-runtime-30-row-seccomp-large-dockerfile`, archive SHA-256 `bfc4c44b2f0da32c812efd1c39ca99361a2efb89ce63e820a38fbe4af20ed600`). Its cleanup-restart row also proved owned containers, networks, volume, and build image stayed deleted after restart. | Repeat the mandatory gate on the final notarized artifact; current evidence is from the isolated development candidate. Dory guarantees its Docker-compatible named-signal contract, not Apple's ContainerAPIClient surface. |
| OrbStack [high memory](https://github.com/orbstack/orbstack/issues/2251), Docker Desktop [high memory](https://github.com/docker/for-mac/issues/6120) and [energy use](https://github.com/docker/for-mac/issues/4323), Colima [`fseventsd` memory/CPU growth from whole-home monitoring](https://github.com/abiosoft/colima/issues/1569) | **LAUNCH BLOCKER — final-artifact certification**. Attributed Dory CPU/RSS/FD/disk sampling, attributed host `fseventsd` RSS/CPU sampling, guest free-page reporting, balloon policy, and daemon-owned Auto-Idle exist. The Health screen exposes blockers/history and daemon-owned in-place repair for DNS, domains, routes, published ports, and the guest agent; Docker API failure remains an explicit workload-aware restart decision. The exact analyzer rejects a late-window `fseventsd` median RSS rise above 128 MiB or final median CPU above 25%, in addition to Dory's own plateau budgets. A 16-client cold-wake herd is served with exactly one engine start. The earlier eight-hour baseline has stable Dory medians but predates narrow on-demand FSEvents roots and host-daemon attribution. The exact soak prevents system sleep without preventing display sleep, requires continuous AC power, and records minute-by-minute power history so host sleep or battery throttling cannot masquerade as Dory resource behavior. | Require the Dory and `fseventsd` plateaus plus an Auto-Idle wake/sleep and subsystem-recovery soak on the exact final candidate. Publish only same-host raw measurements; no unsupported blanket superiority claim. |
| OrbStack [DNS on Sequoia](https://github.com/orbstack/orbstack/issues/1451), [VPN registry](https://github.com/orbstack/orbstack/issues/1305), [custom DNS](https://github.com/orbstack/orbstack/issues/384); Lima [DNS](https://github.com/lima-vm/lima/issues/2939); Colima [DHCP route metric overrides the intended path](https://github.com/abiosoft/colima/issues/1551); Apple container [VPN/MTU](https://github.com/apple/container/issues/345), [network unreachable after VPN activation](https://github.com/apple/container/issues/1307), [custom DNS domain ignored](https://github.com/apple/container/issues/1794), [route/NAT state fails to self-heal after VPN or exit-node churn](https://github.com/apple/container/issues/1881), and [host-wide stale resolver search directive](https://github.com/apple/container/issues/1917) | **LAUNCH BLOCKER — environment certification**. The signed standalone candidate passed active VPN, explicit DNS, registry IPv4, TCP, IPv6-localhost, UDP, container resolver search isolation, and exact host-state preservation (`20260712T012615Z-33591`). The current-source 30-row campaign additionally proved the standard Docker `--dns-search dev.dory.test` contract produces an explicit search domain with a valid nameserver, while a default container retains no stale search directive; both rows are mandatory retained evidence. Dory does not install `/etc/resolver` entries. Publication now requires the direct notarized app to see an active VPN, find the configured corporate resolver in macOS's live resolver contract, and resolve/reach a split-DNS HTTPS endpoint from both host and container. Before sleeping, the dedicated runner must clear any exit node for a baseline, perform three explicitly confirmed Tailscale exit-node enable/disable rounds, keep host/container/Docker connectivity live while the competing route is active, and recover the exact default-route/DNS/search/proxy/resolver contract after each disable. It then preserves the same semantic contract across five physical sleeps and re-verifies hashed private-network inputs without exposing them in the manifest. | Configure the corporate resolver, probe host/URL, and real Tailscale exit node, then execute the mandatory campaign on the real split-DNS/custom-DNS VPN. Clean app/engine shutdown remains required. |
| Colima [IPv6 localhost](https://github.com/abiosoft/colima/issues/583), Lima [IPv6 localhost](https://github.com/lima-vm/lima/issues/1330), Docker Desktop [IPv6](https://github.com/docker/for-mac/issues/1432) | **LAUNCH BLOCKER — exact-artifact/external-route certification**. Current source now has a provenance-pinned dual-stack gvproxy derivative, IPv6 NDP switching, AAAA forwarding, Docker ULA allocation, registry AAAA, IPv6 TCP transport, restart persistence, IPv6-network migration, and published TCP through both `127.0.0.1` and `[::1]`. Both the macOS 15+ raw-HV path and the macOS 14 VZ file-handle-network fallback pass formal disposable fresh/restart, AAAA, IPv6 TCP, dual-loopback publication, explicit-loopback, unpublish, and orphan-cleanup gates. This Mac has no external IPv6 route and is not Sonoma, so the retained VZ manifest is correctly non-release-qualifying instead of faking either condition. | Run the exact notarized arm64 candidate on the dedicated physical macOS 14 runner with both `--require-sonoma` and `--require-external`. The release workflow now blocks publication until that job passes and the published helper digest matches its evidence. |
| OrbStack [bridged/LAN networking](https://github.com/orbstack/orbstack/issues/342), [LAN domains](https://github.com/orbstack/orbstack/issues/814), [hidden remote source IP](https://github.com/orbstack/orbstack/issues/710), [interface-specific privileged ports fail silently](https://github.com/orbstack/orbstack/issues/2567), [LAN and DNS stalls during transient balloon pressure](https://github.com/orbstack/orbstack/issues/2558), [host kernel panic during Docker network I/O](https://github.com/orbstack/orbstack/issues/2562), and [host `configd` watchdog reboot from a helper network-path deadlock](https://github.com/orbstack/orbstack/issues/2585); Apple container [bridge networking](https://github.com/apple/container/issues/489) | **LAUNCH BLOCKER — physical certification**. The Apple-Silicon implementation uses a signed root LaunchDaemon, destination-only PF state, UTUN, an independent framed gvproxy switch port, and conntrack-marked guest policy routing so ordinary TCP/UDP containers receive the original IPv4 source even when LAN/VPN client ranges overlap Docker or gvproxy networks. Host forwarding and PF references are crash-recovered and restored to their exact prior state. Unit, failure-recovery, exact gvproxy bidirectional-L2, payload, and release-policy gates pass. The physical raw-HV gate now additionally reserves a free port below 1024, publishes it to the exact configured LAN/Tailscale interface address, requires the peer to reach it with the same preserved source as ordinary TCP/UDP, and verifies privileged unpublish cleanup; a silent container success without the host listener cannot pass. Both physical paths keep 960 MiB resident in a bounded guest workload while repeatedly proving TCP/UDP source identity, Docker DNS and API deadlines, and bounded macOS `configd` queries: 20 rounds in each raw-HV LAN/Tailscale campaign and 10 dual-peer rounds in the Sonoma VZ campaign. This is not yet FULL without exact notarized-candidate physical-peer evidence and extended host-network churn. | The release workflow requires physical LAN and Tailscale campaigns on both the raw-HV path and the macOS 14 VZ fallback: memory-pressure traffic and DNS/API/configd liveness, exact ordinary and interface-specific privileged TCP/UDP source, explicit-loopback isolation, helper/engine restart recovery, unpublish, PF/route/reference/forwarding cleanup, exact artifact SHA binding, and an unchanged host boot session with no panic report. Run extended interface/DHCP/VPN churn as well because a short gate cannot rule out the reported multi-day host lock inversion. All retained evidence must pass before publication. |
| OrbStack [`orb start` hangs in CI](https://github.com/orbstack/orbstack/issues/765), Colima [start failure](https://github.com/abiosoft/colima/issues/938), [older hang](https://github.com/abiosoft/colima/issues/987), and [2026 arm64 false-running/missing-socket hang](https://github.com/abiosoft/colima/issues/1564); Lima [hang](https://github.com/lima-vm/lima/issues/1609), [unverified network helper causes an infinite start wait](https://github.com/lima-vm/lima/issues/5074), [stale socket/PID state blocks the next boot](https://github.com/lima-vm/lima/issues/5075), [orphaned VZ driver enters a permanent broken-state loop](https://github.com/lima-vm/lima/issues/5087), [a cached VM image cannot boot after a remote HEAD timeout](https://github.com/lima-vm/lima/issues/5188), and [silent Kubernetes worker provisioning failure](https://github.com/lima-vm/lima/issues/5225) | **LAUNCH BLOCKER — final-artifact certification**. Readiness requires the Docker socket and `/_ping`; a VM process alone can never produce a successful start result, and every start has a 120-second deadline with diagnostics. Current source validates PID ownership, removes stale socket nodes without replacing regular files or symlinks, rediscovers pidfile-less dory-hv/dataplane processes only when their commands carry this HOME's exact private state/socket paths, repairs lost PID metadata when that runtime is healthy, and gracefully tears down an incomplete pair before retrying. The gvproxy orphan path is independently reaped, zombie children do not consume the shutdown deadline, and foreign helpers with unrelated state paths remain untouched. Dory has no remote image-freshness HEAD path: the mandatory offline gate boots the exact compressed candidate assets under dead proxy settings, stops gracefully, hides both bundle sources in a disposable clone, and boots again from the unchanged prepared local kernel/rootfs while rejecting any host TCP dependency. The current-source run passed both phases with exact hashes and no host TCP dependency (`~/.dory-new-gate-evidence/20260713T-offline-cached-boot-current`, archive SHA-256 `2a87b1316c7b5441cdc2fa677e993ac14b3e8f9dd9ab5e9e9497834aa782cb57`). Required Linux-machine provisioning is now transactional: doryd rejects nonzero or timed-out install and verify stages, the app deletes a newly created definition when required setup fails, unsupported recipes fail before mutation, and the older container-machine path no longer logs an identity failure and then announces readiness. The exact-candidate machine gate now requires a real `k8s-lab` install plus an independent `kubectl` verification before resource cycling. A separate live run deleted both PID files and repaired them in place, then SIGKILLed only the dataplane and recovered through a guest `powerOff` into a new healthy API; the final stop also ended in `powerOff` (`~/.dory-new-gate-evidence/20260713T-stale-supervisor-self-heal`). The earlier exact signed candidate completed five fresh-home starts in 7–14 seconds and one-second stops, but predates these recovery and provisioning changes. | Repeat fresh starts, cached boot without bundle sources, stale socket/PID cleanup, healthy PID metadata repair, pidfile-less orphan cleanup, single-helper death recovery, bounded stops, and required recipe provisioning/verification on the exact notarized final runtime and self-hosted release runners. Keep Unix socket paths within the platform's 103-byte limit. |
| OrbStack [engine/API wedge under restart churn](https://github.com/orbstack/orbstack/issues/2574), [Compose restart 502](https://github.com/orbstack/orbstack/issues/2570), [Compose one-off steals the service domain](https://github.com/orbstack/orbstack/issues/2576), [ports lost after stop/start](https://github.com/orbstack/orbstack/issues/2553), [stale-route reconcile isolation](https://github.com/orbstack/orbstack/issues/2584), and [infinite retry when macOS owns a host port](https://github.com/orbstack/orbstack/issues/2509); Apple container [`cp` of a missing path wedges later operations](https://github.com/apple/container/issues/1927), [SSH reconnect wedges exec/stop](https://github.com/apple/container/issues/1916), and [per-container hangs cascade into networking](https://github.com/apple/container/issues/1884) | **LAUNCH BLOCKER — final-artifact certification**. The runtime gate passed 20 bounded container restarts; after every cycle it required the published port, inspect, logs, and stats to respond. It also proved missing-source `docker cp` fails within five seconds without wedging exec/inspect/stats, name/port reuse, Compose restart plus stop/start continuity, overlapping-network failure isolation, concurrent backpressure isolation, and full-engine restart identity/policy/port recovery. Dory derives routes from unique Docker container names rather than competitor-specific domain labels, and the exact Compose one-off regression proves a temporary `run` container cannot take over the long-running service route. The same gate caught a standalone restart dropping `--amd64`, which left persisted containers unable to resolve their recorded `dory-runc` OCI runtime. Apple Silicon now defaults the standalone FEX path on and atomically persists CPU, memory, translation, GPU, LAN, and selected data-drive settings; explicit `--no-amd64` remains available. Dory also inspects fixed port bindings before container start: an occupied macOS listener returns a bounded Docker conflict, the container stays stopped, unrelated API calls remain live, and the identical start succeeds once the real host owner releases the port. The cleanup/restart row initially caught the launcher killing gvproxy before the guest could finish its dockerd sync and data-drive unmount, which allowed deleted objects to reappear. The fixed launcher retains gvproxy through guest shutdown, requires `powerOff`, and the deterministic-FEX rootfs repeated all 34 rows with 2,000 connections and 20 restarts while keeping every owned container, network, volume, and build image deleted after restart (`~/.dory-new-gate-evidence/20260713T-deterministic-fex-final/runtime-regression`). The physical sleep campaign now owns an isolated machine, holds a real interactive shell open before every sleep, disconnects that client after wake, then requires bounded fresh status/exec, stop/start, and persistent-disk access. Publication verifies all five session records and binds `dorydctl`, kernel, and rootfs hashes to the exact candidate. | Run the mandatory runtime and five-cycle physical machine-session campaigns on the final notarized artifact and retain their bounded-command evidence. Missing session output, a wedged stop, failed restart, lost disk marker, mismatched machine assets, resurrected objects, or silent host-port collision must block publication. |
| Apple container [declines first-party Compose support](https://github.com/apple/container/issues/55), [still lacks a Compose workflow](https://github.com/apple/container/issues/1846), and [does not surface a one-shot container's last exit code](https://github.com/apple/container/issues/1501); Podman Desktop [downloads Compose unsuccessfully behind corporate TLS](https://github.com/podman-desktop/podman-desktop/issues/13497); Docker Compose regressions involving [external networks on `down`](https://github.com/docker/compose/issues/9145), [profiles on `down`](https://github.com/docker/compose/issues/8139), [`--remove-orphans`](https://github.com/docker/compose/issues/11203), [multi-file volume teardown](https://github.com/docker/compose/issues/5246), [`!reset` merge semantics](https://github.com/docker/compose/issues/10697), and [non-TTY progress](https://github.com/docker/compose/issues/13099) | **FULL** — Dory ships its signed, pinned Docker Compose v2 helper and the GUI delegates validation, `up`, `start`, `stop`, `restart`, and data-safe `down` to that same official implementation. The incomplete in-app YAML/interpolation/reconcile clone has been removed. Every GUI invocation pins Dory's exact Unix socket, strips inherited Compose control variables, uses no shell, drains stdout/stderr concurrently into bounded buffers, has cancellation and a deadline, disables interactive menus, and withholds raw failure output that may contain secrets. Project actions recover only from exact native Compose project/working-directory/config-file/environment-file labels; name-prefix guesses fail closed. `down` enables every profile, removes genuine retired Compose services, removes project networks, preserves external networks and named user data, and never adds `-v` implicitly. The strengthened disposable-engine gate passed all 38 rows with the bundled v2.39.2 helper, including multi-file `.env`/`!reset` merge, completed/healthy dependencies, profile isolation, bind/named-volume/custom/external-network wiring, absolute recovery labels, real orphan cleanup, bounded lifecycle/logs, engine restart, and deletion persistence. A failed one-shot fixture exits 37, and Dory must report that exact value through `docker wait`, `docker inspect .State.ExitCode`, and the `exited=37` list filter, so Compose can distinguish successful and failed completion dependencies. Release evidence records and verifies the exact Compose-helper SHA-256 independently of the Docker CLI. | Keep the 14 focused process/metadata/concurrency tests, the complete live `compose-v2-lifecycle` row, exact nonzero exit-code proof, exact helper digest binding, structural release checks, and clean-install UI replay mandatory. Any Compose upgrade must repeat merge/profile/orphan/external-network/data-preservation and non-TTY output tests before it can ship. |
| OrbStack [machine CPU-limit change crashes the app](https://github.com/orbstack/orbstack/issues/2591) | **LAUNCH BLOCKER — exact-artifact machine reconfiguration certification**. Dory edits machine CPU/memory in a bounded 1–8 CPU and 1–16 GiB UI, and `doryd` enforces the identical boundary before stopping or changing a running machine. The focused 23-test manager selection passes. The rebuilt signed Xcode UI scheme now expands Advanced, automatically reveals the controls above the footer, exercises 2→8→1 CPUs and 2→16→1 GiB, verifies both bounds disable, and passes inside the complete 8/8 UI suite. The isolated runtime gate cycles 1→8→2 CPUs and 1→16→4 GiB, verifies guest-visible resources and persistent disk state after each automatic restart, then proves 9 CPUs and 512 MiB fail without mutation. | Run the runtime gate through the exact public Developer ID/notarized app, daemon, and machine assets and retain their binary hashes; source-level UI and daemon coverage are complete, but only that exact-artifact execution can close publication. |
| Apple container [no first-class machine resource statistics](https://github.com/apple/container/issues/1919) | **FULL** — Dory samples the machine guest directly through its bounded guest-agent exec path and exposes a strict versioned `dorydctl machine stats` JSON contract for CPU, used/total memory, network RX/TX, block read/write, process count, and uptime. The app refreshes real CPU and used/total memory every two seconds; unavailable values remain `—` rather than being replaced with allocated memory or fabricated `0.0%`. Exact-field/range parser tests, XPC round trips, UI-model lifecycle tests, and the complete app/core suites pass. The mandatory machine resource gate validates this schema and guest-memory consistency after every 1→8→2 CPU / 1→16→4 GiB restart. | Keep parser, XPC, UI-model, and exact-candidate runtime proofs mandatory. Reject unknown/missing fields, impossible ranges, loopback-inclusive network accounting, scientific-notation counter corruption, or a successful resource restart without valid live stats. |
| OrbStack [active long-lived TCP connections drop after exactly 24 hours](https://github.com/orbstack/orbstack/issues/2468) and [Linux-machine-to-Docker protocol round trips degrade to 200/400 ms](https://github.com/orbstack/orbstack/issues/2587); Rancher Desktop [intermittently drops only outbound VM packets](https://github.com/rancher-sandbox/rancher-desktop/issues/6943) | **LAUNCH BLOCKER — duration certification**. The publication-bound soak keeps one published TCP connection active for 25 hours, heartbeats without reconnecting, and records the unchanged tuple. In parallel, a privileged, machine-labeled container matching Dory's managed-machine network shape repeatedly resolves `host.docker.internal` and reaches a second published Docker service while independently resolving a fresh IPv4 address and making a deadline-bounded external TCP connection to `registry-1.docker.io:443`. Publication recomputes both raw TSVs: the local route requires at least 90% of expected 30-second samples, p99 at or below 100 ms, no three-sample run at or above 150 ms, exact bytes, and a live machine; outbound TCP permits at most 0.5% isolated failures and no two consecutive failures. Both paths must exceed 24 hours. The current-source eight-second triple-path harness passed eight host heartbeats, eight zero/10 ms machine-to-service samples, and eight external TCP connections with no failure (`~/.dory-new-gate-evidence/20260713T-machine-outbound-short-live`, archive SHA-256 `571363d475bcbbd5c2221f8777bd7c4596a6125d980d4f112e32a541c99cbaf6`). A display-neutral `caffeinate -is` assertion and minute AC-power history must span the measured connection; publication semantically verifies both instead of trusting a completion flag. | Run the full 90,000-second triple-path gate against the final exact candidate. Short development runs validate the harness and routes only; they cannot prove the uptime-dependent failures absent. |
| Colima [emulated x86 unreachable](https://github.com/abiosoft/colima/issues/962) and [Rosetta VZ profile loses its network address](https://github.com/abiosoft/colima/issues/1556), OrbStack [GNU tar breaks on nested directories in amd64/Rosetta](https://github.com/orbstack/orbstack/issues/2588), Apple container [wrong-platform pull](https://github.com/apple/container/issues/83) and [default pull unpacks every platform while image sizes disagree](https://github.com/apple/container/issues/1537) | **FULL for the advertised common-amd64-image contract and default arm64 pull** — Dory runs amd64 containers inside the same networked native Apple-silicon VM rather than creating a separate x86 VM/profile. The exact app gate verifies the container-local `binfmt_misc` view and performs an amd64 Node/npm BuildKit build, tests/runtime, and GNU tar create/extract of nested directories with hard-link preservation. Separately, the mandatory fresh-store gate pulls a digest-pinned multi-platform index without `--platform`, requires the selected local image and default container to be arm64, and semantically reconciles `image inspect`, `/images/json`, `/system/df`, and layer bytes. Docker's inspect-size definition may differ from storage bytes, so the gate requires image-list and system-df equality plus a bounded 16× cross-definition ratio rather than asserting false equality. Current source selected arm64 and reported 4,131,356 inspect bytes versus an exactly matching 13,408,336 bytes from both storage APIs, a 3.245× attributable ratio (`~/.dory-new-gate-evidence/20260713T-default-arm64-image-current`, archive SHA-256 `53482aa679d45644fc4169b17c9796b307e06af6cda86be7ce4fbcaf5608ae95`). | Keep both gates mandatory. Product-specific x86-only workloads outside the named release fixtures need their own qualification rather than being inferred from the common-amd64 contract. A cached image cannot qualify default selection; the arm64 gate must start from a store where the exact image is absent. |
| Docker Desktop [stale bind-file size](https://github.com/docker/for-mac/issues/7501), Apple container [intermittent single-file mounts](https://github.com/apple/container/issues/1251), and Lima [spaces in mount paths silently fail](https://github.com/lima-vm/lima/issues/5136) | **FULL** — the dedicated bind-coherence gate uses a host path containing spaces and validates both a directory bind and an independent direct single-file bind through every same-inode shrink, grow, and same-size content refresh. It preserves native Linux semantics across atomic replacement: the directory view follows the new path while the direct bind remains pinned to the old inode, then a fresh direct attachment sees the replacement. Guest-to-host truncation passes through the direct bind, and 20 complete direct-file container recreations read the exact final bytes. The current-source run passed with graceful `powerOff` (`~/.dory-new-gate-evidence/20260713T-spaced-direct-bind-current-final`). The gate is now mandatory in final qualification; retained publication verification checks every phase, all three size/digest views, the 20-cycle count, image pin, Docker CLI digest, and result digest. HostFS/FUSE attribute-refresh tests cover the same paths below the Docker layer. | Keep the spaced-directory, direct-file, Linux atomic-replacement, fresh-reattachment, same-inode coherence, and 20-cycle reliability contract mandatory on the final notarized arm64 artifact. |
| Apple container [prune failure](https://github.com/apple/container/issues/891) | **FULL (exact signed candidate)** — a fail-closed gate requires the exact `ISOLATED-ENGINE-PRUNE` token and an engine with zero pre-existing containers, volumes, and custom networks. On the exact candidate it ran unfiltered system/container/image/network/volume/BuildKit prunes: the running container, attached network, image, and volume bytes survived; every unused victim and all build cache were removed (`20260712T013219Z-42935`). The gate now emits source/image/client-bound evidence, proves owned cleanup, and is mandatory in exact qualification and retained publication verification. | Never run this destructive gate against a user engine; keep it mandatory on the dedicated release account and notarized final bits. |
| Apple container [registry auth](https://github.com/apple/container/issues/254), [ECR manifest PUT returns 401](https://github.com/apple/container/issues/1707), and [ECR partial-blob retry returns 416](https://github.com/apple/container/issues/1895); Apple containerization [blob retry restarts at byte zero](https://github.com/apple/containerization/issues/790); OrbStack [Docker login credential-save failure](https://github.com/orbstack/orbstack/issues/2457) and [Docker Hub manifest requests return EOF while MCR works](https://github.com/orbstack/orbstack/issues/2578) | **FULL (exact notarized candidate)** — the local private-registry gate proves rejected anonymous pull, isolated login, authenticated pull/run, a normal BuildKit build with a non-leaking secret mount, push, inspect/history, identity-preserving save/load, tag/remove, filtered prune, and complete credential/object cleanup. It now pulls a digest-pinned arm64 registry fixture, binds evidence to the source plus exact Docker/Buildx hashes, and is mandatory in exact qualification and retained publication verification. The fresh-store default-platform gate performs a real digest-pinned Docker Hub pull without `--platform`, so a manifest-path EOF cannot hide behind cache. The exact notarized 0.3.0/18 runtime and bundled Docker/Buildx clients also pass against real ECR: the gate creates an incompressible 96 MiB layer, observes live push progress, terminates the first upload with a nonzero result, resumes successfully, repeats the manifest PUT, removes the local image, repulls and runs the exact checksum, deletes the unique remote tag, logs out, and removes the isolated credential directory (`/tmp/dory-ecr-exact-evidence-20260714T062401Z/20260714T062417Z-27505/manifest.txt`). That campaign exposed and fixed the gate hiding bundled Buildx behind its isolated `DOCKER_CONFIG`; qualification and publication require both candidate client hashes. GitHub Actions uses a main/tag-restricted OIDC role that can access only the dedicated empty qualification repository, with no stored AWS access keys and a one-day orphan lifecycle backstop. | Keep the local auth/image-lifecycle gate and real ECR interruption/resume, repeated PUT, repull/run checksum, exact-client hashes, and local/remote credential cleanup mandatory. Keep the OIDC trust restricted to `Augani/dory` main and `v*` tags and the IAM policy restricted to the qualification repository. |
| Apple container [missing healthcheck runtime support](https://github.com/apple/container/issues/1918), [missing named BuildKit contexts](https://github.com/apple/container/issues/1930), and [failure to expand a defaulted Dockerfile ARG](https://github.com/apple/container/issues/1766) | **FULL** — Dory passes Docker healthcheck configuration and runtime history, proves `starting→healthy`, repeated failure to `unhealthy`, `NONE`, start interval/period, and cleanup, and builds with a named `--build-context` copied by Dockerfile `COPY --from`. Four overlapping builds must additionally consume independent required secrets and produce only their own exact context bytes. The mandatory offline fixture assigns the digest-qualified base a run-unique local repository, builds the competitor's exact `ARG TAG="${TAG:-latest}"` form without a build-arg override or registry fallback, and executes the exact output. The 34-row current-source campaign passed with 2,000 connections and 20 restarts (`~/.dory-new-gate-evidence/20260713T-deterministic-fex-final/runtime-regression`). | Keep the semantic runtime checks mandatory on the exact notarized artifact; a successful CLI exit without inspectable health history, named-context bytes, expanded default value, or concurrent-session isolation is not sufficient. |
| Apple container [layered `.dockerignore` ordering differs from Docker](https://github.com/apple/container/issues/1800) | **FULL** — Dory uses the standard Docker/BuildKit context matcher. The mandatory live gate reproduces the nested Rails-style ignore/unignore ordering, requires both root and nested `.gitkeep` files in the built image, proves both excluded siblings are absent, and rejects an out-of-order or otherwise failed context stream. | Keep the exact nested include/exclude byte checks mandatory on the notarized artifact. |
| Apple container [`image save` corrupts a stdout archive with trailing references](https://github.com/apple/container/issues/1801) and [layer extraction rejects a hard link whose parent has no explicit tar entry](https://github.com/apple/container/issues/1797) | **FULL** — the bounded offline gate saves the digest-qualified fixture through stdout, parses the tar itself rather than trusting a permissive reader, requires `manifest.json`, two complete EOF blocks, and a zero-only remainder. It then constructs a two-member layer containing a deep hard link with no parent-directory records, imports it, exports the stopped container, and requires exact bytes plus identical host inode identity for the source and link. The current-source archive ended at byte 4,250,112 with exactly 1,024 zero bytes after it; the hard-link import/export preserved identity and exact bytes. Every imported image and container was run-owned and removed (`~/.dory-new-gate-evidence/20260713T-deterministic-fex-final/runtime-regression`). | Keep strict stdout framing, missing-parent synthesis, hard-link identity, bounded import/export, and exact cleanup mandatory on the notarized artifact. Do not accept a load-only round trip that silently ignores trailing bytes. |
| Apple container [internal DNS failure on a clean install](https://github.com/apple/container/issues/856), [missing network-scoped aliases](https://github.com/apple/container/issues/1839), [container-to-container DNS discovery](https://github.com/apple/container/issues/1809), and [unnecessary IP changes on restart](https://github.com/apple/container/issues/1836) | **FULL** — Dory's Docker API preserves native network contracts and attachment-scoped aliases. The bounded lifecycle gate verifies bridge options, internal/attachable flags, explicit subnet/range/gateway/auxiliary IPAM, labels, exact filtered listing, duplicate-name rejection without mutation, static-IP plus alias connect, active-endpoint removal rejection, disconnect, explicit remove, port liveness, prune safety, and deletion persistence after engine restart. A separate clean-network path requires the primary container name plus `db` and `database` to resolve and serve exact bytes, proves those aliases do not resolve from the default network, and preserves the identical endpoint IP and every name after stop/start. The integrated development replay passed every row. | Keep the complete lifecycle, clean-network DNS, alias scope, exact response, restart-IP equality, active-network prune safety, and cleanup persistence mandatory. Extend the same proof whenever multi-network Compose attachment logic changes. |
| Apple container [`cp` hangs or no-ops through a mounted named volume](https://github.com/apple/container/issues/1873), [lacks Docker-compatible `cp -` tar streams](https://github.com/apple/container/issues/1908), and [fresh named volumes contain `lost+found` and break database initialization](https://github.com/apple/container/issues/1730) | **FULL** — before writing fixture bytes, the bounded live gate requires a new named-volume root to contain no entries. It verifies local driver/options/labels by inspection, exact label-filtered listing, and Docker's same-name idempotency without metadata mutation. While a consumer is running, removal must fail within ten seconds, report the in-use conflict, and preserve the marker bytes. The exact public Docker archive boundary streams `docker cp container:/data/. -` directly into `docker cp - container:/data` without relying on `tar` inside either container. An independently scanned manifest then proves regular files, directories, hard links, symlinks, FIFO, sparse layout, modes, uid/gid, timestamps, binary xattrs, and non-UTF-8 names survived; sockets are deliberately excluded and the source remains unchanged. The integrated runtime gate also copies a deterministic 1 MiB payload from the host into a mounted volume and back, compares exact bytes, requires unrelated Docker API liveness, explicitly removes a second volume, and proves cleanup stays deleted after engine restart. Active volume bytes also survive the mandatory unfiltered prune gate. Both the full 34-row campaign and the integrated development replay passed. | Keep metadata, filter, collision, in-use rejection, fresh-root emptiness, both `cp -` tar-stream directions, independent manifest equality, ordinary host copy directions, byte equality, post-copy API liveness, prune safety, and restart-persistent cleanup mandatory. A hidden root entry, metadata mutation, hang, unsupported archive stream, or lost byte is a release failure. |
| OrbStack [custom images](https://github.com/orbstack/orbstack/issues/11), [DKMS](https://github.com/orbstack/orbstack/issues/258) | **FULL for Dory's published contract** — Linux Machines accept explicit kernel/rootfs pairs through a signed Ed25519 manifest contract; tampered manifests and artifacts fail before `dorydctl` runs. The public recipe rebuilds the same SHA-256-pinned arm64 or amd64 kernel, rootfs, and guest agent used by Dory releases, verifies their source fingerprints, architecture, required tools, and embedded-agent digest, and signs the exact pair. The bundled baseline is recursively app-signed and payload-inventoried. | Arbitrary DKMS inside the immutable production Docker guest remains intentionally unsupported. Custom external images must provide Dory's required virtual-hardware drivers and guest-agent contract; this row does not promise that an arbitrary distribution image boots unchanged. |
| Docker Desktop [malware/signing warning](https://github.com/docker/for-mac/issues/7520) and [in-app update does not install the latest version](https://github.com/docker/for-mac/issues/7511), plus Apple container [installer package fails signature validation](https://github.com/apple/container/issues/1682) | **LAUNCH BLOCKER — final-source distribution rebuild**. The app and nested executables pass strict Developer ID verification, quarantine cleanup is scoped, and public-output validation rejects XCTest/UI-test runners from the app and Sparkle archive. Exact notarized build 34 from `35ddb8cd63599c3367decd329115ba632f5029a0` passed recursive signature, ticket, Gatekeeper, and SBOM validation from the direct ZIP, app-update ZIP, lite ZIP, and mounted DMG. Its quarantined DMG path passed translocated first launch, Docker readiness, uninstall-preserves-data, and clean-user restoration; its Homebrew path passed install, first boot, normal uninstall, reinstall, zap, durable-drive preservation, Docker-plugin preservation, and profile restoration. The real Sparkle 2.9.4 updater verified the Ed25519 archive, terminated the old process, atomically installed the exact tree, relaunched a different PID, revalidated Gatekeeper/SBOM/ticket, preserved settings and app data, and restored the clean release account. | Rebuild from the final source commit and repeat the three distribution gates because release-gate and cask fixes after build 34 intentionally supersede its source binding. Retain the exact app-tree, PID, version, key, install, and state evidence; any `codesign`, `spctl`, Gatekeeper, Homebrew, or updater disagreement blocks release. |
| Lima [release SBOM artifacts](https://github.com/lima-vm/lima/issues/5219) | **FULL** — every public Dory release now includes a deterministic CycloneDX 1.6 SBOM with a SHA-256 record for every regular file and symlink in the exact shipped app, its portable path/type/mode/size, GPL identity, source commit, and complete app-tree digest. The SBOM is itself hash-bound in the release manifest and published beside the app. Validation regenerates the inventory from the final app and rejects missing, extra, runner-local, duplicate, or false records even if an attacker recomputes the outer manifest hash. | Keep SBOM generation, semantic verification, qualification, and publication mandatory. Extend the component metadata as additional independently bundled ecosystems require richer license/package attribution. |
| OrbStack [sandbox/agent compatibility](https://github.com/orbstack/orbstack/issues/2295), [Docker-container SSH-agent forwarding](https://github.com/orbstack/orbstack/issues/741), [ingress-only NetworkPolicy breaks DNS/TCP egress](https://github.com/orbstack/orbstack/issues/2583), [Nix GC fails with EPERM in `linux/amd64`](https://github.com/orbstack/orbstack/issues/2538), and [`mmdebstrap` fails under Rosetta](https://github.com/orbstack/orbstack/issues/2543); Colima [nondeterministic nested-container segfault](https://github.com/abiosoft/colima/issues/1550), [Kubernetes API has no route to the advertised VM address](https://github.com/abiosoft/colima/issues/1339), and [M1 Kubernetes API port repeatedly flaps](https://github.com/abiosoft/colima/issues/1595); Apple container [SSH/home mapping](https://github.com/apple/container/issues/205), [BuildKit SSH-agent mounts](https://github.com/apple/container/issues/1472), [BuildKit without Rosetta](https://github.com/apple/container/issues/1825), [relative BuildKit context failure in a temporary directory](https://github.com/apple/container/issues/1899), [BuildKit permissions failure](https://github.com/apple/container/issues/1906), [Dockerfiles above 16 KiB close the builder transport](https://github.com/apple/container/issues/735), [pacman cannot install its own seccomp sandbox](https://github.com/apple/container/issues/1628), and [missing in-container seccomp](https://github.com/apple/container/issues/1915) | **LAUNCH BLOCKER — final-artifact certification**. Testcontainers 12.0.4, Dev Containers CLI 0.87.0, `act` 0.2.89, LocalStack 4.14.0, Tilt 0.37.5, nested k3s 1.36.2, Skaffold 2.23.0, and Supabase CLI 2.109.1 all pass version/image-bound semantic gates with exact cleanup. The Kubernetes gate advertises the nested control plane only on a Dory-owned loopback port, rewrites the kubeconfig to that reachable endpoint, requires a Ready node, and then records 60 consecutive one-second host `/readyz` successes before any workload tooling begins; a single refusal, route failure, `starting`, or `ServiceUnavailable` response fails the candidate. The mandatory runtime gate builds from a relative dot context inside its run-scoped temporary tree, named BuildKit contexts and secret mounts are exercised, and the nested k3s gate applies an ingress-only NetworkPolicy then proves service DNS and TCP egress from the selected pod. It also builds and runs an exact image from a 65,582-byte Dockerfile and requires Docker to advertise seccomp before a custom native-AArch64 profile blocks `mkdir`/`mkdirat` with errno 13 while unrelated reads and the Docker API remain live. Apple Silicon now uses one provenance-pinned FEX 2607 runtime rather than qemu-user or a Rosetta sidecar. Its static-PIE interpreter/server pair is mounted read-only through Dory's fail-closed OCI wrapper on Docker and BuildKit's direct runc path. The generic patch preserves canonical x86 shebang and `/usr/bin/env` chains, private interpreter-state isolation, proc-less nested chroots, descriptor-exec arguments including null `argv`, and inherited guest seccomp through child ELFs. A fresh gate requires those invariants in BuildKit, Docker run, and `docker exec` with only the production x86_64 `POCF` handler. The same exact FEX binary passed OrbStack #2543's unmodified `mmdebstrap --variant=minbase trixie /tmp/rootfs.tar`, a subsequent proc-less chroot shebang, and Apple #1628's unmodified `RUN pacman -Sy --noconfirm fzf` with alpm's sandbox enabled. Hash `b862d2a4358b102b125ae50da357b189a5d4710a3be830ef3280cba400c7099b` was reproduced by two forced-fresh builds from the pinned Ubuntu snapshot and 210-package inventory; app bundling no longer permits a host qemu-user fallback. Nix 2.34.7 GC and digest-pinned Node/npm BuildKit/runtime also pass on the rebuilt rootfs, with retained evidence at `~/.dory-new-gate-evidence/20260713T-deterministic-fex-final`. Dory exposes the Docker Desktop-compatible guest socket `/run/host-services/ssh-auth.sock` through a dedicated same-user vsock bridge on raw-HV and macOS 14 VZ; arbitrary host sockets remain rejected by virtiofs. The SSH gate performs a network-disabled required mount, compares public-key-listing digests, exercises concurrent clients, and repeats VZ after restart without retaining key text. | Repeat the complete compatibility, one-minute Kubernetes API stability, generic exec, `mmdebstrap`, Node/BuildKit/Nix/Arch, large-Dockerfile, and SSH-agent set on the exact notarized artifact. Any Kubernetes API flap, sandbox workaround, unpinned FEX input, leaked private marker, qemu-user fallback, missing cleanup, or incomplete evidence blocks publication. |
| BuildKit [registry-cache cancellation can leave a hot hung daemon](https://github.com/moby/buildkit/issues/6008) and [local image export can hang while cache/goroutines grow](https://github.com/moby/buildkit/issues/6209), plus Buildx [stuck client after builder death](https://github.com/docker/buildx/issues/556) | **LAUNCH BLOCKER — final-artifact certification**. The GUI now invokes Dory's bundled Buildx directly against the exact Dory socket/default builder, gives it the context directory instead of buffering an in-memory tar, honors `.dockerignore`, streams bounded plain progress, and terminates the helper when the user cancels or dismisses the sheet. The mandatory 39-row runtime gate binds the Buildx SHA-256 independently, exports/imports a local cache, requires actual `CACHED` reuse, interrupts an active `RUN`, requires the client to exit within ten seconds, rejects any published cancelled image, then completes a fresh build and bounded Docker API call without restarting the engine. The authenticated private-registry gate separately exports and imports a registry cache with the same isolated credentials and exact helper; both cache directions, secret non-leakage, image lifecycle, credentials, and object cleanup passed in a disposable candidate-engine replay. | Build and notarize the final-source app, repeat the 39-row production thresholds and authenticated registry-cache gate through that exact artifact, then keep the helper digest, bounded cancellation, post-cancel solve/API liveness, cache cleanup, and UI cancel/close replay mandatory. |

## Release decision implied by this matrix

Every row above is machine-checked as exactly `FULL` or `LAUNCH BLOCKER`; there is no percentage,
partial credit, or intermediate strategy state. The product is not yet release-complete. Public release remains
**NO-GO** while any Apple Silicon launch blocker above is open: physical sleep, source-IP-preserving
LAN networking, exact-artifact external IPv6, external-drive binds, bind advisory locks, machine resource reconfiguration,
corporate split-DNS/VPN, Gatekeeper/Homebrew,
the final-artifact eight-hour and 25-hour soaks, and exact-artifact migration/rollback/compatibility/SSH-agent
replay. Only `FULL` rows may ship, and Intel is not a blocker for the arm64 release.
